# dependencies
node_modules
admin/node_modules

# build output
dist
admin/dist

# env files — keep secrets out of git
.env
.env.local
.env.*.local
admin/.env
admin/.env.local
admin/.env.*.local

# editor / OS
.DS_Store
.vscode
.idea
*.log

# vite
.vite
admin/.vite
.vercel

# deck QA artefacts (regenerable from build-deck.mjs)
presentation/qa-images
presentation/CGS-Porter-Pitch.pdf

# audit run artefacts (regenerable from scripts/audit-full.py)
presentation/audit-artifacts

# cgs-qa-loop run state (per-run logs + history) — local cache, regenerable
.claude/skills/cgs-qa-loop/state/run-*
.claude/skills/cgs-qa-loop/state/latest
.claude/skills/cgs-qa-loop/state/history.jsonl
.claude/skills/cgs-qa-loop/state/known-issues.json
